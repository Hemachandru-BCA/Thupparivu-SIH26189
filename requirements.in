# requirements.in — Input file for `pip-compile`.
# Run: pip-compile requirements.in -o requirements.txt --strip-extras
# This produces a fully-pinned requirements.txt that CI can install from.

# --- Core pipeline (generator / preprocessing) ---
pandas>=2.0.0
pydantic>=2.0.0
python-dateutil>=2.9.0
langdetect>=1.0.9
Pillow>=10.0.0
pytesseract>=0.3.10
Faker>=20.0.0

# --- Extraction (NER / relation / event extraction) ---
spacy>=3.7.0

# --- Graph (resolution / graph_builder / analytics / ghost_nodes / embeddings) ---
networkx>=3.0
numpy>=1.24.0

# --- XAI layer (evidence / findings / dossiers) ---
# pydantic already required above; no extra deps for the offline mock LLM.

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Ingestion extras (optional) ---
# pypdf>=4.0.0

# --- Tests ---
pytest>=7.4.0
httpx>=0.27.0

# --- Auth (optional but recommended in production) ---
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4

# --- Indian name transliteration ---
Indic-transliteration>=2.3.57
