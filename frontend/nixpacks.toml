[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm-8"]

[phases.build]
cmds = ["pnpm install", "pnpm build"]

[phases.deploy]
cmds = ["vite preview --host 0.0.0.0 --port $PORT"]

[variables]
NODE_VERSION = "20"