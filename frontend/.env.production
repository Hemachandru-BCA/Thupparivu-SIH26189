# Production build environment (used by `vite build` in GitHub Actions).
# These values are committed — they are PUBLIC configuration (only the
# backend URL + base path), no secrets.

# Render backend URL (no trailing slash)
VITE_API_URL=https://thupparivu-sih26189.onrender.com

# GitHub Pages base path = repository name
BASE_PATH=/Thupparivu-SIH26189